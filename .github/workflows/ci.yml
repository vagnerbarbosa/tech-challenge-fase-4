name: Unit & Integration Tests

on:
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Testes
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar Poetry
        uses: snok/install-poetry@v1
        with:
          version: latest
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Carregar dependências em cache
        id: cached-poetry-dependencies
        uses: actions/cache@v4
        with:
          path: .venv
          key: venv-${{ runner.os }}-3.11-${{ hashFiles('**/poetry.lock') }}-v2

      - name: Instalar dependências
        if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
        run: poetry install --no-interaction --no-root

      - name: Instalar projeto
        run: poetry install --no-interaction

      - name: Executar linters (Ruff)
        run: poetry run ruff check src/ tests/

      - name: Executar type checker (mypy)
        run: poetry run mypy src/

      - name: Executar testes
        run: poetry run pytest tests/ -v --ignore=tests/e2e/ --cov=src --cov-report=xml --cov-fail-under=70
        env:
          PYTHONPATH: ${{ github.workspace }}
          AZURE_TEXT_KEY: fake-key-for-testing
          AZURE_TEXT_ENDPOINT: http://localhost:3001/

      - name: Upload coverage report
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
